The only reason these runners exist is to make debugging easier when a test fails.
You can't run them in IE because it won't allow access to local storage
To run in chrome, close all instances of chrome and start chrome from a prompt like this: chrome --allow-file-access-from-files